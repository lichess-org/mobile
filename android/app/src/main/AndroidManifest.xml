<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="Lichess"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:fullBackupContent="@xml/backup_rules">

        <meta-data
          android:name="io.flutter.embedding.android.EnableImpeller"
          android:value="false" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_lichess_notification" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/SplashTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="false"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/x-chess-pgn" />
                <data android:mimeType="application/vnd.chess-pgn" />
                <data android:mimeType="text/x-chess-pgn" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />

                <data android:pathSuffix=".pgn" />
            </intent-filter>
            <intent-filter>
               <action android:name="android.intent.action.SEND" />
               <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-chess-pgn" />
                <data android:mimeType="application/vnd.chess-pgn" />
                <data android:mimeType="text/x-chess-pgn" />
            </intent-filter>

            <!-- App Links verified against https://lichess.org/.well-known/assetlinks.json -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" android:host="lichess.org" />

                <data android:pathPattern="/training/....." />
                <data android:pathPattern="/study/........" />
                <data android:pathPattern="/study/......../........" />
                <data android:pathPattern="/tournament/........" />

                <!--
                  User profile links: /@/<username> where usernames are 2–30 chars.
                  Fixed-length patterns are used instead of /@/.* to exclude sub-paths
                  like /@/username/blog/... which are not supported in the app.
                  pathPattern matches the full path so /@/.. only matches a 2-char username,
                  not /@/username/blog/slug/id.
                -->
                <data android:pathPattern="/@/.." />
                <data android:pathPattern="/@/..." />
                <data android:pathPattern="/@/...." />
                <data android:pathPattern="/@/....." />
                <data android:pathPattern="/@/......" />
                <data android:pathPattern="/@/......." />
                <data android:pathPattern="/@/........" />
                <data android:pathPattern="/@/........." />
                <data android:pathPattern="/@/.........." />
                <data android:pathPattern="/@/..........." />
                <data android:pathPattern="/@/............" />
                <data android:pathPattern="/@/............." />
                <data android:pathPattern="/@/.............." />
                <data android:pathPattern="/@/..............." />
                <data android:pathPattern="/@/................" />
                <data android:pathPattern="/@/................." />
                <data android:pathPattern="/@/.................." />
                <data android:pathPattern="/@/..................." />
                <data android:pathPattern="/@/...................." />
                <data android:pathPattern="/@/....................." />
                <data android:pathPattern="/@/......................" />
                <data android:pathPattern="/@/......................." />
                <data android:pathPattern="/@/........................" />
                <data android:pathPattern="/@/........................." />
                <data android:pathPattern="/@/.........................." />
                <data android:pathPattern="/@/..........................." />
                <data android:pathPattern="/@/............................" />
                <data android:pathPattern="/@/............................." />
                <data android:pathPattern="/@/.............................." />
                <data android:pathPattern="/tv/.*" />

                <data android:pathPattern="/broadcast/.*/.*/........" />
                <data android:pathPattern="/broadcast/.*/.*/......../........" />

                <!--
                  Game or challenge link: any 8-char path (e.g. /abcd1234).
                  pathPattern cannot exclude known static 8-char routes (e.g. /analysis,
                  /settings); the app resolver handles false positives by falling back to
                  launchUrl. On Android 15+, exclusions are enforced server-side via
                  dynamic_app_link_components in assetlinks.json.
                -->
                <data android:pathPattern="/........" />

                <!-- Game with color POV -->
                <data android:pathPattern="/......../black" />
                <data android:pathPattern="/......../white" />
            </intent-filter>
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false" />
        </activity>
        <!-- OAuth callback receiver for flutter_web_auth_2. The modern Chrome Auth Tab returns the
             result directly via an activity result; this activity is the fallback path used when the
             auth session falls back to a plain Custom Tab. Two filters:
              - custom scheme org.lichess.mobile://login-callback — used on dev/staging hosts where
                HTTPS App Links aren't verified,
              - HTTPS App Link https://lichess.org/account/oauth/mobile-callback — used in production
                once lichess redirects there (domain verified via /.well-known/assetlinks.json).
             android:taskAffinity="" keeps the callback in the app's task (advised by the package). -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="org.lichess.mobile" android:host="login-callback" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="lichess.org"
                    android:path="/account/oauth/mobile-callback" />
            </intent-filter>
        </activity>

        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- On Android 11+ (API 30) package visibility is restricted: the app can only see/resolve other
         apps it explicitly declares intents for here. See:
         https://developer.android.com/training/package-visibility -->
    <queries>
        <!-- Lets the Flutter engine resolve text-processing apps (io.flutter.plugin.text.ProcessTextPlugin), see:
             https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets url_launcher discover a browser/Custom Tabs handler for https. Required for OAuth
             login: without it, launchUrl() can't resolve a browser on Android 11+ and returns false,
             so the in-app browser never opens and sign-in fails. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
